PORT=8080
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/masterontime?schema=public"
JWT_SECRET="replace-with-a-long-random-string"
JWT_EXPIRES_IN="1d"

# Перший ADMIN — створюється командою `npm run seed:admin`
ADMIN_EMAIL="admin@masterontime.local"
ADMIN_PASSWORD="replace-with-a-long-random-password"
